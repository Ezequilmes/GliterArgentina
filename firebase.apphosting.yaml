runtime: nodejs20
entrypoint: npm start

env:
  - key: NODE_ENV
    value: production
  - key: PORT
    value: "8080"

build:
  command: npm ci && npm run build
  environment:
    - key: NODE_ENV
      value: production